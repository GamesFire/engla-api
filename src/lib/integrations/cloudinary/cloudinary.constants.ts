/**
 * Cloudinary core settings and folder structures.
 */
export const CloudinaryConfig = {
  FORMAT: 'webp',
  QUALITY: 'auto',
  FOLDERS: {
    AVATARS: 'avatars',
    PROPERTIES: 'properties',
  },
} as const;
